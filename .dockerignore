# Never bake these into the image.
node_modules
dist
.git
.gitignore

# Secrets — must never enter the image.
.env
.env.*
!.env.example

# Regenerated at runtime.
uploads/objects
public/salesbook-pages

# Cruft.
*.tar.gz
.DS_Store
npm-debug.log*
*.log
.vscode
.idea
